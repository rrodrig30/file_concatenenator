# Flask Configuration
FLASK_HOST=0.0.0.0
FLASK_PORT=5000
FLASK_DEBUG=False

# Security
SECRET_KEY=your-secret-key-change-in-production

# Application Settings
MAX_FILE_SIZE=10485760  # 10MB in bytes
INCLUDED_EXTENSIONS=.py,.txt,.md,.html,.css,.js,.java,.json,.xml,.yaml,.yml,.cfg,.conf,.log,.env,.example,.gitignore,.dockerfile,.sql,.sh,.bat

# Security Settings for Directory Browsing
# Comma-separated list of allowed base paths for browsing
# Leave empty to allow browsing from user's home directory only
ALLOWED_BROWSE_PATHS=

# Performance Settings
NUM_WORKERS=8
CHUNK_SIZE=65536
MAX_FILE_SIZE=10485760

# Logging
LOG_DIR=logs